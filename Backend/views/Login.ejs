<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Page</title>
  <!-- Bootstrap CSS -->
<%- include("Includes/link.ejs"); %>
  <style>
    .login-card {
      border:2px solid black;
      box-shadow: -10px 0 15px -5px rgba(0,0,0,0.4); 
      border-radius: 12px;
      background: #fff;
      padding: 30px;
      transition: 0.3s ease-in-out;
    }
    .login-card:hover {
      box-shadow: -15px 0 20px -5px rgba(0,0,0,0.5);
      transform: scale(1);
    }
  </style>
</head>
<body class="bg-light">
  <div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="col-10 col-sm-8 col-md-6 col-lg-4">
      <div class="login-card">
     <form action="http://localhost:8000/login" method="post">
        <h3 class="text-center mb-4">Login up for visit us</h3>

        <div class="mb-3">
          <input type="email" name="email" id="em" class="form-control" placeholder="Enter your email" required>
        </div>

        <div class="mb-3">
          <input type="password" name="password" id="password" class="form-control" placeholder="Enter your password" required>
        </div>

        <div class="d-grid">
   <button type="submit" class="btn btn-warning" >Submit</button>
        </div>
      </form>
    </div>
  </div>
</div>
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
