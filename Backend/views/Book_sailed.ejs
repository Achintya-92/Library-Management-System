<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> Sail Book</title>
  <!-- Bootstrap CSS -->
    <link rel="icon" type="image/x-icon" href="/library.ico">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<body>
  <div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="col-10 col-sm-8 col-md-6 col-lg-4">
      <div class="login-card">
        
     <form action="http://localhost:8000/books/id/buy/borrow" method="post">
        <h3 class="text-center mb-4">Enter Borrower Details to buy Book!</h3>

        <div class="mb-3">
          <input type="text" name="b_name" id="name" class="form-control" placeholder="Enter Borrower name" value="<%= old ? old.b_name : '' %>" required>
        </div>
          <% if (errors && errors.b_name) { %>
           <p style="color:red" class="para"><%= errors.b_name %></p>
      <% } %>
        <div class="mb-3">
              <input type="email" name="b_email" class="form-control" placeholder="Email" value="<%= old ? old.b_email : '' %>" required>
            </div>
            <% if (errors && errors.b_email) { %>
   <p style="color:red" class="para"><%= errors.b_email %></p>
<% } %>
        <div class="mb-3">
          <input type="number" name="No" id="No" class="form-control" placeholder="Enter Borrower Mobile Number" value="<%= old ? old.No : ''%>" required>
        </div>
                    <% if (errors && errors.No) { %>
   <p style="color:red" class="para"><%= errors.No %></p>
<% } %>
        <div class="mb-3">
          <input type="number" name="amount" id="amount" class="form-control" placeholder="Enter Amount" value="<%= old ? old.amount : ''%>" required>
        </div>
                            <% if (errors && errors.amount) { %>
   <p style="color:red" class="para"><%= errors.amount %></p>
<% } %>
                <div class="mb-3">
          <input type="date" name="b_date" id="b_date" class="form-control" placeholder="Enter Borrowing date"  value="<%= old ? old.b_date : ''%>" required>
                </div>
                                            <% if (errors && errors.b_date) { %>
   <p style="color:red" class="para"><%= errors.b_date %></p>
<% } %>
                <div class="mb-3">
          <input type="date" name="r_date" id="r_date" class="form-control" placeholder="Enter Return date" value="<%= old ? old.r_date : ''%>"  required>
                </div>
                            <% if (errors && errors.r_date) { %>
   <p style="color:red" class="para"><%= errors.r_date %></p>
<% } %>
        <div class="mb-3">
            <% if (book.bookid) { %>
          <input type="hidden" name="bookid" id="bookid" value="<%= book.bookid %>">
           <% } else { %>
       <input type="hidden" name="bookid" id="bookid" value="<%=old ? old.bookid : '' %>">
       <%}%>
        </div>

        <div class="d-grid">
   <button type="submit" class="btn btn-primary" >Submit</button>
        </div>
                  <% if (message) { %>
   <p style="color:red" class="para"><%= message  %></p>
<% } %>
      </form>
    </div>
  </div>
</div>
<script>
  console.log("<%= book.bookid %>")
</script>
</body>
</html>
