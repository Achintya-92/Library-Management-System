<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User List</title>
<%- include("Includes/link.ejs"); %>
  <style>
    table,th,td{
      border: 1px solid black;
    }
  </style>
</head>
<body style="padding:10px;">
  <h1>Borrower Details</h1>
  <table class="table mt-3">
  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Email</th>
            <% if (user.no) { %>
        <th>Mob. No.</th>
      <%}%>
       <% if (user.review) { %>
      <th>Review</th>
      <%}%>
      <th>Edit</th>
    </tr>
  </thead>
  <tbody>
 <% if (user) { %>
  <tr>
                <% if (user.id) { %>
        <td><%= user.id %></td>
      <%}%>
                      <% if (user.b_id) { %>
        <td><%= user.b_id %></td>
      <%}%>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
                <% if (user.no) { %>
            <td><%= user.no %></td>
      <%}%>
           <% if (user.review) { %>
           <td><%= user.review %></td>
      <%}%>
    <td>
      <form action="/user/<%= user.id %>/edit" method="post">
        <h1></h1>
        <input type="hidden" name="name" value="<%= user.name %>">
        <input type="hidden" name="email" value="<%= user.email %>">
        <button class="btn btn-sm btn-warning">Edit Username</button>
      </form>
    </td>
  </tr>
<% } else { %>
  <tr>
    <td colspan="5">❌ No user found</td>
  </tr>
<% } %>
  </tbody>
  </table>
</body>
</html>
