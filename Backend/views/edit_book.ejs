<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Books Stored</title>
    <link rel="icon" type="image/x-icon" href="/library.ico">
</head>
<style>
  form{
    align-items: center;
  }
</style>
<body>
   
 
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Page</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .add-card {
        border:1px solid black;
      box-shadow: -10px 0 15px -5px rgba(0,0,0,0.4); 
      border-radius: 12px;
      background: #fff;
      padding: 30px;
      transition: 0.3s ease-in-out;
    }
    .add-card:hover {
      box-shadow: -15px 0 20px -5px rgba(0,0,0,0.5);
      transform: scale(1);
    }
  </style>
</head>
<body class="bg-light">
  <div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="col-10 col-sm-8 col-md-6 col-lg-4">
        <div class="add-card">
<form action="http://localhost:8000/updateBook" method="post">
  <div class="mb-3">
    <input type="text" name="book_name" id="book_name" class="form-control" placeholder="<%= book.book_name %>"  value="<%= book.book_name %>">
  </div>
  <div class="mb-3">
    <input type="text" name="author_name" id="author_name" class="form-control"  value="<%= book.author_name %>">
  </div>
  <div class="mb-3">
    <input type="text" name="edition" id="edition" class="form-control" value="<%= book.edition %>">
  </div>
    <div class="mb-3">
    <input type="text" name="price" id="price" class="form-control" value="<%= book.price %>">
  </div>
      <div class="mb-3">
    <input type="text" name="row_no" id="row_no" class="form-control" value="<%= book.row_no %>">
  </div>
  <div class="mb-3">
    <input type="text" name="col_no" id="col_no" class="form-control" value="<%= book.col_no %>">
  </div>
        <div class="mb-3">
    <input type="text" name="count" id="count" class="form-control" value="<%= book.count %>">
  </div>
          <div class="mb-3">
    <input type="hidden" name="book_id" id="book_id" class="form-control" value="<%= book.book_id %>">
  </div>
  <button type="submit" class="btn btn-warning form-control-sm">Update Book</button>
</form>
 </div>
</div>
</div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
app.post("/editBook",(req,res)=>{
  const {book_id} = req.body;
  let q = "select * from books where book_id = ?";
  db.query(q,[book_id],(err,book)=>{
    if(err){
      console.log(err);
    }
res.render("edit_book.ejs", {book});
});
});

